const bulletins = require('./bulletins.json');
const employees = require('./employees.json');
const activities = require('./activities.json');

module.exports = {
  bulletins,
  employees,
  activities
}